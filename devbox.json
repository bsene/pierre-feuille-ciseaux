{
  "$schema":  "https://raw.githubusercontent.com/jetify-com/devbox/0.10.7/.schema/devbox.schema.json",
  "packages": ["gambit@latest"],
  "shell": {
    "init_hook": [
      "if whence -w gsi | grep alias;then unalias gsi;fi"
    ],
    "scripts": {
      "test": [
        "clear",
        "gsi pierre_feuille_ciseaux_test.scm"
      ]
    }
  }
}
